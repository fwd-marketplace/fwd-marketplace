# === API del BackEnd ===
# URL base del BackEnd (Express). El FrontEnd consume TODO por aquí vía fetch.
# En local, el BackEnd corre en el puerto 3001.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# NOTA: el FrontEnd NO usa claves de Supabase. La conexión a Supabase vive
# solo en el BackEnd. No agregar SUPABASE_URL ni SUPABASE_KEY aquí.
